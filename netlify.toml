[build]
  base = "client"
  command = "npm install; npm install --prefix ../server; npm run build"
  publish = "dist"
  functions = "../server/netlify/functions"

[build.environment]
  NODE_VERSION = "18"